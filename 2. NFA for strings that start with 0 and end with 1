#include <stdio.h>
#include <string.h>

int main() {
    char str[100];

    printf("Enter binary string: ");
    scanf("%s", str);

    int len = strlen(str);

    if(str[0]=='0' && str[len-1]=='1')
        printf("Accepted");
    else
        printf("Rejected");

    return 0;
}
